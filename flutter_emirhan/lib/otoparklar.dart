import 'package:flutter/material.dart';

class OtoparklarPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Otoparklar Sayfası"),
      ),
      body: Center(
        child: Text("Bu Otoparklar Sayfası"),
      ),
    );
  }
}
